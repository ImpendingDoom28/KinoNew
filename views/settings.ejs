<link href="/css/styleSettings.css" rel="stylesheet" type="text/css">
<% include header.ejs %>
<div class="container2">
    <div class='content'>
        <div class='welcome'><h1>❤Добро пожаловать в настройки <%= typeof name != 'undefined' ? ', ' + user.name : '' %>
                !❤️
                Здесь вы сможете настроить новинки по своим предпочтениям!📽🎞️</h1></div>
        <div class='input-fields'>
            <form method="post" action="/account/settings">
                <label for="favGenre">Список любимых жанров:</label>
                <datalist id="favGenre" onload="getGenres()">
                    <option>УЖАСЫ</option>
                    <option>ПРИКЛЮЧЕНИЯ</option>
                    <option>КОМЕДИЯ</option>
                    <option>СЕМЕЙНЫЙ</option>
                    <option>МУЛЬТФИЛЬМ</option>
                    <option>БОЕВИК</option>
                    <option>ФАНТАСТИКА</option>
                    <option>ДОКУМЕНТАЛЬНЫЙ</option>
                    <option>ВОЙНА И ПОЛИТИКА</option>
                    <option>ДРАМА</option>
                    <option>НФ И ФЭНТЕЗИ</option>
                    <option>ТРИЛЛЕР</option>
                    <option>ДЕТЕКТИВ</option>
                </datalist>
                    <ul id="favGenresList">
                        <li>
                            <input
                                    id="favGenre"
                                    name="favGenre"
                                    list="favGenre"
                                    placeholder="Введите жанр"
                                    class='input-line full-width'
                                    onchange="addNewRow()"
                                    value="<%= typeof favGenre != 'undefined' ? favGenre : '' %>"
                            />
                        </li>
                    </ul>
                <label for="favActors">Список любимых актёров:</label>
                <ul id="favGenresList">
                    <li>
                        <input
                                id="favActors"
                                name="favActors"
                                list="favActors"
                                placeholder="Введите имя актёра"
                                class='input-line full-width'
                                value="<%= typeof favActors != 'undefined' ? favActors : '' %>"
                        />
                    </li>
                </ul>
                <label for="countries">Список стран производителей:</label>
                <ul id="favGenresList">
                    <li>
                        <input
                                id="countries"
                                name="countries"
                                list="countries"
                                placeholder="Введите страну"
                                class='input-line full-width'
                                value="<%= typeof countries != 'undefined' ? countries : '' %>"
                        />
                    </li>
                </ul>
                <label for="minRating">Минимальный рейтинг фильма/сериала(по 100 балльной шкале):</label>
                <input
                        id="minRating"
                        name="minRating"
                        list="minRating"
                        placeholder="Введите рейтинг"
                        class='input-line full-width'
                        value="<%= typeof minRating != 'undefined' ? minRating : '' %>"
                />
                <div class="button">
                    <input type="submit" value="Сохранить" class="ghost-round full-width">
                </div>
            </form>
        </div>
    </div>
</div>